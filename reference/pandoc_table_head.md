# Table head / body / foot

Table head / body / foot

## Usage

    pandoc_table_head(attr = <object>, rows = list())

    pandoc_table_body(
      attr = <object>,
      row_head_columns = 0L,
      head_rows = list(),
      body_rows = list()
    )

    pandoc_table_foot(attr = <object>, rows = list())
