# Table

Table

## Usage

    pandoc_table(
      source_info = <object>,
      attr = <object>,
      caption = <object>,
      colspec = list(),
      head = <object>,
      bodies = list(),
      foot = <object>
    )
