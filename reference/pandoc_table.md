# Table

Table

## Usage

``` r
pandoc_table(
  source_info = pandoc_source_info(),
  attr = pandoc_attr(),
  caption = pandoc_caption(),
  colspec = list(),
  head = pandoc_table_head(),
  bodies = list(),
  foot = pandoc_table_foot()
)
```
