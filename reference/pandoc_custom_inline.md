# Custom inline node (Quarto extensions)

Custom inline node (Quarto extensions)

## Usage

``` r
pandoc_custom_inline(
  source_info = pandoc_source_info(),
  type_name = "",
  slots = list(),
  attr = pandoc_attr()
)
```
