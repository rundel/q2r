# Custom inline node (Quarto extensions)

Custom inline node (Quarto extensions)

## Usage

    pandoc_custom_inline(
      source_info = <object>,
      type_name = "",
      slots = list(),
      attr = <object>
    )
