# Custom block node (Quarto extensions: callouts, tabsets, ...)

Custom block node (Quarto extensions: callouts, tabsets, ...)

## Usage

    pandoc_custom_block(
      source_info = <object>,
      type_name = "",
      slots = list(),
      attr = <object>
    )
