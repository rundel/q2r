# Custom block node (Quarto extensions: callouts, tabsets, ...)

Custom block node (Quarto extensions: callouts, tabsets, ...)

## Usage

``` r
pandoc_custom_block(
  source_info = pandoc_source_info(),
  type_name = "",
  slots = list(),
  attr = pandoc_attr()
)
```
