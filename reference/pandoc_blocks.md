# Typed list wrappers

`pandoc_blocks()` and `pandoc_inlines()` validate that every element of
the supplied list is of the appropriate kind.

## Usage

``` r
pandoc_blocks(content = list())

pandoc_inlines(content = list())
```
