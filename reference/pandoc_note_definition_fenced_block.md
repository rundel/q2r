# Note definition (fenced block form)

Note definition (fenced block form)

## Usage

    pandoc_note_definition_fenced_block(
      source_info = <object>,
      id = "",
      content = <object>
    )
