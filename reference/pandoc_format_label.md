# Label a pandoc AST node for tree display

Generic returning a single-line label for a node. Override per class to
include node-specific summary information (level, url, type, ...).

## Usage

``` r
pandoc_format_label(x, ...)
```

## Arguments

- x:

  An AST node.
