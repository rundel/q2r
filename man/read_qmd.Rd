% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_qmd}
\alias{read_qmd}
\alias{write_qmd}
\alias{edit_qmd}
\title{Read, write, and edit QMD files}
\usage{
read_qmd(path, ast = c("pd", "ts"), ...)

write_qmd(x, path)

edit_qmd(path, .f, ...)
}
\arguments{
\item{path}{Path to a \code{.qmd} file.}

\item{ast}{Which AST to build, \code{"pd"} (Pandoc, default) or \code{"ts"}
(tree-sitter); passed to \code{\link[=parse_qmd]{parse_qmd()}}.}

\item{...}{Further arguments passed to \code{\link[=parse_qmd]{parse_qmd()}}.}

\item{x}{A \code{\link{pandoc}} or \code{\link{ts_tree}} object to render.}

\item{.f}{A function (or rlang formula) taking the parsed AST and
returning the edited AST.}
}
\value{
\code{read_qmd()} returns a \code{\link{pandoc}} or \code{\link{ts_tree}};
\code{write_qmd()} and \code{edit_qmd()} return the (edited) AST invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Thin file-oriented conveniences around \code{\link[=parse_qmd]{parse_qmd()}} and
\code{\link[=to_qmd]{to_qmd()}} that close the parse-edit-write loop (parsermd's
\code{as_document()} role):
\itemize{
\item \code{read_qmd()} parses a file path (and, unlike \code{\link[=parse_qmd]{parse_qmd()}}, errors
if the path does not exist rather than treating it as inline text).
\item \code{write_qmd()} renders an AST with \code{\link[=to_qmd]{to_qmd()}} and writes it
verbatim, with no added trailing newline so the round trip stays
byte-faithful. Returns its input invisibly so it can end a pipe.
\item \code{edit_qmd()} is the in-place one-liner: read, apply \code{.f}, write back.
}
}
\examples{
path = tempfile(fileext = ".qmd")
writeLines("# Title\n\nSome text.\n", path)

read_qmd(path)

edit_qmd(path, \(d) map_nodes(d, is(pandoc_header),
                              .f = \(h) add_class(h, "done")))
cat(readLines(path), sep = "\n")

}
