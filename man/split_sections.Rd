% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toc.R
\name{split_sections}
\alias{split_sections}
\title{Split a document into per-section sub-documents}
\usage{
split_sections(x, level = 1L, ...)
}
\arguments{
\item{x}{A \code{\link{pandoc}} document, a \code{\link{pandoc_blocks}} wrapper, or a list
of blocks.}

\item{level}{Heading level at which to split (default \code{1}). Headings at
other levels stay inside their enclosing section.}

\item{...}{Unused; for future extension.}
}
\value{
A named \code{list} of \code{\link{pandoc}} documents.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Partitions the top-level block stream of a \code{\link{pandoc}} document at every
heading of a given \code{level}, returning a named list of \code{\link{pandoc}}
documents (one per section, named by the heading text), the q2r analog
of mq's \code{section::split()}. Any blocks before the first boundary
heading become a leading preamble document named \code{""}.

Each returned document can be rendered with \code{\link[=to_qmd]{to_qmd()}},
\code{\link[=render_qmd]{render_qmd()}}, or written out; wrap the list in a
\code{\link{qmd_collection}} if you want to run the batch verbs over it.
}
\examples{
\dontrun{
doc = parse_qmd("intro\n\n# A\n\na\n\n# B\n\nb\n")
parts = split_sections(doc, level = 1)
names(parts)
}

}
