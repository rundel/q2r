% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{table_df}
\alias{table_df}
\alias{as_df}
\alias{as_table}
\title{Convert between pandoc tables and data frames}
\usage{
as_df(x, ...)

as_table(df, caption = NULL, align = NULL, id = "")
}
\arguments{
\item{x}{A \code{\link{pandoc_table}} (returns one \code{data.frame}), or a
\code{\link{pandoc}} / \code{\link{pandoc_blocks}} / list of blocks (returns a list of
\code{data.frame}s, one per table found in document order).}

\item{df}{A \code{data.frame} (or object coercible to one).}

\item{caption}{Table caption as a string, or \code{NULL} for none. Defaults
to the \code{"q2r_caption"} attribute of \code{df} if present.}

\item{align}{Column alignments, one of \code{"left"}, \code{"right"},
\code{"center"}, or \code{"default"} per column (recycled to the number of
columns). Defaults to the \code{"q2r_align"} attribute of \code{df}, else
\code{"default"}.}

\item{id}{Table identifier (the Quarto \verb{#tbl-} label). Defaults to the
\code{"q2r_id"} attribute of \code{df}, else \code{""}.}
}
\value{
\code{as_df()} returns a \code{data.frame} (or list thereof);
\code{as_table()} returns a \code{\link{pandoc_table}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Bridge the \code{\link{pandoc_table}} node and the data structure R users work
in. \code{as_df()} flattens a parsed table into a \code{data.frame} (the
header row becomes column names, each cell is rendered to plain text
with \code{\link[=ast_text]{ast_text()}}); \code{as_table()} builds a \code{\link{pandoc_table}} from a
\code{data.frame} so it can be spliced into a document and written back with
\code{\link[=to_qmd]{to_qmd()}}. Together they are the q2r analog of mq's \verb{table::} module.

Column alignments, the table caption, and the table identifier ride
along on the returned \code{data.frame} as the attributes \code{"q2r_align"},
\code{"q2r_caption"}, and \code{"q2r_id"}, and \code{as_table()} reads them back when
its own \code{align} / \code{caption} / \code{id} arguments are left at their
defaults. A \code{as_df()} then \code{as_table()} round trip therefore
preserves alignment and caption without extra bookkeeping.

Cells are reduced to plain text, so inline formatting (emphasis, links,
code) inside a cell is dropped. Cell row and column spans are not
modelled: each cell maps to exactly one column.

Only the first header row becomes the column names; any further header
rows and any body group-header rows are flattened into ordinary data
rows.
}
\examples{
\dontrun{
doc = parse_qmd("| a | b |\n|--:|:--|\n| 1 | x |\n")
df = as_df(doc)[[1]]
as_table(df, caption = "demo")
}

}
