% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{qmd_collection}
\alias{qmd_collection}
\alias{parse_qmd_dir}
\alias{write_qmd_dir}
\title{A collection of QMD documents}
\usage{
qmd_collection(docs = list(), paths = character(0))

parse_qmd_dir(
  dir = ".",
  pattern = "\\\\.qmd$",
  recurse = TRUE,
  ast = c("pd", "ts"),
  quiet = TRUE,
  prune_errors = TRUE
)

write_qmd_dir(x, dir = NULL)
}
\arguments{
\item{dir}{For \code{parse_qmd_dir()}, the directory to scan. For
\code{write_qmd_dir()}, the destination directory; \code{NULL} (the default)
writes each document back to the path it was read from.}

\item{pattern}{A regular expression selecting files (default
\code{"\\\\.qmd$"}).}

\item{recurse}{Whether to descend into subdirectories.}

\item{ast}{Which AST to build, \code{"pd"} (default) or \code{"ts"}, passed to
\code{\link[=parse_qmd]{parse_qmd()}}.}

\item{quiet}{Suppress per-file diagnostic signaling (default \code{TRUE};
diagnostics are still attached to each document's \verb{@diagnostics}).}

\item{prune_errors}{Passed to \code{\link[=parse_qmd]{parse_qmd()}}.}

\item{x}{A \code{qmd_collection}.}
}
\value{
\code{parse_qmd_dir()} returns a \code{qmd_collection}. \code{write_qmd_dir()}
returns its input invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{qmd_collection} is a lightweight container holding several parsed
\code{\link{pandoc}} documents, modeled on parsermd's document collections. It
lets you parse a directory of \code{.qmd} files once and then run the same
selection and rewriting verbs across every document in a single pipe.

The selection and mutation verbs from \code{\link[=select_nodes]{select_nodes()}} dispatch on a
collection:
\itemize{
\item Selection verbs (\code{select_nodes}, \code{select_descendants},
\code{select_children}, \code{select_first}) map over the documents and return
a named list of per-document results (so you keep track of which
document each match came from).
\item Mutation verbs (\code{map_nodes}, \code{replace_nodes}, \code{delete_nodes},
\code{splice_nodes}, \code{insert_before}, \code{insert_after}) rewrite every
document and return a new \code{qmd_collection}.
\item \code{walk_nodes} applies a side effect to every document and returns the
collection invisibly.
\item \code{\link[=ast_summary]{ast_summary()}} returns a combined \code{data.frame} with a leading
\code{doc} column naming the source document.
}
}
\examples{
\dontrun{
coll = parse_qmd_dir("docs", pattern = "\\\\.qmd$")
coll |>
  map_nodes(is(pandoc_header), .f = \(h) add_class(h, "tagged")) |>
  write_qmd_dir("docs-tagged")
ast_summary(coll)
}

}
