% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts-query.R
\name{ts_query}
\alias{ts_query}
\title{Run a tree-sitter \code{.scm} query against QMD source}
\usage{
ts_query(input, query_text)
}
\arguments{
\item{input}{Either a \code{\link{ts_tree}} (re-renders to QMD via \code{\link[=to_qmd]{to_qmd()}}
then re-parses), a single string treated as text/file path (per
\code{\link[=pampa_parse]{pampa_parse()}}'s rules), or raw text.}

\item{query_text}{A tree-sitter query string (S-expression form).
See \url{https://github.com/quarto-dev/q2/tree/main/crates/tree-sitter-qmd/tree-sitter-markdown/queries/}
for live examples.}
}
\value{
A list with one element per match. Each match is itself a
named list whose names are the capture identifiers in \code{query_text}
and whose values are \code{\link{ts_node}} objects. Returns \code{list()} when no
matches are found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Power-user escape hatch alongside \code{\link[=select_nodes]{select_nodes()}}. Compiles
\code{query_text} against the tree-sitter-qmd grammar and returns one
entry per match, with captures keyed by name.

This bypasses the tidyselect-style mask entirely; use \code{\link[=select_nodes]{select_nodes()}}
if a CSS/predicate query is enough. \code{ts_query()} is the right tool when
you need full structural pattern matching, captures, or predicates
(\verb{#eq?}, \verb{#match?}, ...) from the tree-sitter query language.
}
\examples{
\dontrun{
ts = pampa_parse("# Heading\n\nbody\n", ast = "ts")
ts_query(ts, "(atx_heading) @h")
}
}
