% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast-construct.R
\name{ast_construct}
\alias{ast_construct}
\alias{as_inlines}
\alias{as_blocks}
\title{Coerce flexible input into pandoc_inlines or pandoc_blocks}
\usage{
as_inlines(x)

as_blocks(x)
}
\arguments{
\item{x}{A character vector, a single inline/block node, a list of
nodes, or a \code{pandoc_inlines}/\code{pandoc_blocks} wrapper.}
}
\value{
A \code{\link{pandoc_inlines}} or \code{\link{pandoc_blocks}} wrapper.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Smooth over the verbosity of pandoc constructors by accepting plain
strings, single nodes, lists of nodes, or already-wrapped sequences,
and producing the canonical wrapper type. Inspired by Pandoc Lua
filters, where constructors like \code{pandoc.Para("hi")} coerce strings
to inline sequences automatically.

For \code{as_inlines()}:
\itemize{
\item A character vector is split on whitespace, producing
\code{\link{pandoc_str}} runs joined by \code{\link[=pandoc_space]{pandoc_space()}}; embedded
newlines (or multi-element character vectors) become
\code{\link[=pandoc_soft_break]{pandoc_soft_break()}}.
\item A single \code{\link{pandoc_inline}} is wrapped in \code{\link{pandoc_inlines}}.
\item A list is validated and wrapped.
\item An existing \code{\link{pandoc_inlines}} is returned as-is.
}

For \code{as_blocks()}:
\itemize{
\item A character vector becomes one \code{\link{pandoc_paragraph}} per non-empty
element (each paragraph's content is \code{as_inlines(line)}).
\item A single \code{\link{pandoc_block}} is wrapped in \code{\link{pandoc_blocks}}.
\item A list is validated and wrapped.
\item An existing \code{\link{pandoc_blocks}} is returned as-is.
}

These exist as ergonomic shortcuts for use inside \code{\link[=ast_filter]{ast_filter()}}
handlers and ad-hoc AST construction; the strict-typed constructors
(\code{\link{pandoc_inlines}}, \code{\link{pandoc_blocks}}, \code{\link{pandoc_str}}, ...) remain
the canonical way to build nodes.
}
\examples{
\dontrun{
pandoc_emph(content = as_inlines("hello world"))
as_blocks(c("first paragraph", "second paragraph"))
}

}
