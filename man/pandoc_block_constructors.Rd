% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd-ast-block.R
\name{pandoc_block_constructors}
\alias{pandoc_block_constructors}
\alias{pandoc_plain}
\alias{pandoc_paragraph}
\alias{pandoc_line_block}
\alias{pandoc_code_block}
\alias{pandoc_raw_block}
\alias{pandoc_block_quote}
\alias{pandoc_ordered_list}
\alias{pandoc_bullet_list}
\alias{pandoc_definition_list}
\alias{pandoc_header}
\alias{pandoc_horizontal_rule}
\alias{pandoc_figure}
\alias{pandoc_div}
\alias{pandoc_table}
\alias{pandoc_block_metadata}
\alias{pandoc_note_definition_para}
\alias{pandoc_note_definition_fenced_block}
\alias{pandoc_caption_block}
\alias{pandoc_custom_block}
\title{Block constructors}
\usage{
pandoc_plain(content = pandoc_inlines(list()))

pandoc_paragraph(content = pandoc_inlines(list()))

pandoc_line_block(content = list())

pandoc_code_block(attr = pandoc_attr(), text = "")

pandoc_raw_block(format = "", text = "")

pandoc_block_quote(content = pandoc_blocks(list()))

pandoc_ordered_list(attr = pandoc_list_attributes(), content = list())

pandoc_bullet_list(content = list())

pandoc_definition_list(content = list())

pandoc_header(
  level = 1L,
  attr = pandoc_attr(),
  content = pandoc_inlines(list())
)

pandoc_horizontal_rule()

pandoc_figure(
  attr = pandoc_attr(),
  caption = pandoc_caption(),
  content = pandoc_blocks(list())
)

pandoc_div(attr = pandoc_attr(), content = pandoc_blocks(list()))

pandoc_table(
  attr = pandoc_attr(),
  caption = pandoc_caption(),
  colspec = list(),
  head = pandoc_table_head(),
  bodies = list(),
  foot = pandoc_table_foot()
)

pandoc_block_metadata(meta = pandoc_meta_value())

pandoc_note_definition_para(id = "", content = pandoc_inlines(list()))

pandoc_note_definition_fenced_block(id = "", content = pandoc_blocks(list()))

pandoc_caption_block(content = pandoc_inlines(list()))

pandoc_custom_block(type_name = "", slots = list(), attr = pandoc_attr())
}
\arguments{
\item{content}{The node's children: a \code{\link{pandoc_inlines}} or
\code{\link{pandoc_blocks}} wrapper, or a list of wrappers for the multi-item
containers (lists, line blocks, definition lists).}

\item{attr}{A \code{\link{pandoc_attr}} (a \code{\link{pandoc_list_attributes}} for
\code{pandoc_ordered_list()}).}

\item{text}{Verbatim text content (code blocks, raw blocks).}

\item{format}{Output format name of a raw block (e.g. \code{"html"}).}

\item{level}{Heading level, 1-6.}

\item{caption}{A \code{\link{pandoc_caption}}.}

\item{colspec}{A list of \code{\link{pandoc_col_spec}} objects, one per column.}

\item{head, foot}{A \code{\link{pandoc_table_head}} / \code{\link{pandoc_table_foot}}.}

\item{bodies}{A list of \code{\link{pandoc_table_body}} objects.}

\item{meta}{A \code{\link{pandoc_meta_value}} (parse-only, see Notes).}

\item{id}{Note-definition identifier.}

\item{type_name}{Custom node type name (parse-only, see Notes).}

\item{slots}{Custom node payload (never populated, see Notes).}
}
\value{
An S7 object of the named block class.
}
\description{
Constructors for the concrete Pandoc block-level node classes. Each returns
an S7 object extending \link{pandoc_block}. See \link{pandoc_node} for the abstract
hierarchy and \link{pandoc_inline_constructors} for the inline nodes.
}
\section{Notes}{

\code{pandoc_block_metadata()} is parse-only and lossy: the reader never
populates its \verb{@meta} slot (it stays the empty default) and pampa's
writer does not emit it, so it round-trips as an empty node. The slot
is kept for forward compatibility.

\code{pandoc_custom_block()} (and \code{pandoc_custom_inline()}) \verb{@slots} and
\verb{@attr} are never populated from a parse: the Rust exporter emits only
the node's \code{type_name}, so custom-node content is currently invisible
to the R side, and \code{\link[=to_qmd]{to_qmd()}} cannot write custom nodes back.
}

\seealso{
\link{pandoc_inline_constructors}, \link{pandoc_node}, \link{pandoc_support_types}
}
