% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pampa.R
\name{pampa_to_qmd}
\alias{pampa_to_qmd}
\title{Render QMD through pampa's own QMD writer}
\usage{
pampa_to_qmd(input)
}
\arguments{
\item{input}{A single string, a \code{\link{ts_tree}}, a \code{\link{pandoc}} object, or
a tagged list.}
}
\value{
A single string with the rendered QMD. Diagnostics (if any)
are attached as \code{attr(result, "diagnostics")}.
}
\description{
Testing helper that invokes \verb{pampa::writers::qmd::write} and returns
the resulting QMD text. Accepts:
\itemize{
\item a single string (text or file path, handled like
\code{\link[=pampa_parse_pd]{pampa_parse_pd()}}) - parsed with pampa (ts -> Pandoc), then written;
\item a \code{\link{ts_tree}} - rendered to QMD via \code{\link[=to_qmd]{to_qmd()}} and fed through
the text path. This is the full
R -> ts_ast -> text -> Pandoc -> qmd pipeline;
\item a \code{\link{pandoc}} object or tagged-list AST - reconstructed directly into
pampa's \code{Pandoc} value and written out (skips the ts -> Pandoc step).
}
}
\details{
Intended for comparing against the R-side \code{to_qmd()} implementations.
}
