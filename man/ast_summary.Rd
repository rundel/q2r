% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast-summary.R
\name{ast_summary}
\alias{ast_summary}
\title{Tabular overview of a document's top-level blocks}
\usage{
ast_summary(x, max_text = 40L)
}
\arguments{
\item{x}{A \code{\link{pandoc}} document, a \code{\link{pandoc_blocks}} wrapper, or a
plain \code{list} of top-level blocks.}

\item{max_text}{Maximum width of the truncated \code{text} preview.}
}
\value{
A \code{data.frame} with columns \code{type} (S7 class name), \code{level}
(header level or \code{NA}), \code{id} (\verb{@attr@id} or \code{NA}), \code{section}
(deepest enclosing heading title, from \code{\link[=ast_sections]{ast_sections()}}), \code{text}
(truncated \code{\link[=ast_text]{ast_text()}} preview), and \code{node} (a list-column of the
block objects). The \code{node} column displays as compact \verb{<type>}
placeholders but holds the live S7 objects for piping back through
the verbs.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Returns a one-row-per-top-level-block \code{data.frame} summarising a
\code{\link{pandoc}} document, the analog of parsermd's \code{as_tibble.rmd_ast()}.
It makes a document graspable at a glance and bridges to base/dplyr
filtering: the \code{node} list-column holds the live S7 objects, so a
filtered frame can be fed straight back through the selection and
mutation verbs.

The view is deliberately shallow (top-level blocks only) to mirror
parsermd's flat model; reach for \code{\link[=select_descendants]{select_descendants()}} to inspect
nested content.
}
\examples{
\dontrun{
doc = parse_qmd("# A\n\nsome text\n\n## B\n\nmore\n")
ast_summary(doc)
}

}
