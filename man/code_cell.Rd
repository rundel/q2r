% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell.R
\name{code_cell}
\alias{code_cell}
\alias{is_code_cell}
\alias{cell_engine}
\alias{cell_options}
\alias{cell_code}
\alias{cell_label}
\alias{set_cell_options}
\alias{set_cell_label}
\alias{collect_code}
\title{Quarto code-cell helpers}
\usage{
is_code_cell(x)

cell_engine(x)

cell_options(x)

cell_code(x)

cell_label(x)

set_cell_options(x, ...)

set_cell_label(x, value)

collect_code(x, engine = NULL, eval_only = FALSE, label_comments = TRUE)
}
\arguments{
\item{x}{A \code{\link{pandoc_code_block}} (for the accessors / setters) or any
node or document (for \code{collect_code()}).}

\item{...}{For \code{set_cell_options()}, named \code{key = value} option pairs.}

\item{value}{For \code{set_cell_label()}, the new label string.}

\item{engine}{For \code{collect_code()}, keep only cells with this engine.}

\item{eval_only}{For \code{collect_code()}, drop cells whose \code{eval} option
is \code{FALSE}.}

\item{label_comments}{For \code{collect_code()}, prefix each cell's code
with a \verb{# <label>} comment when it has a label.}
}
\value{
Accessors return a scalar (or named list, for
\code{cell_options()}); setters return a new \code{\link{pandoc_code_block}};
\code{collect_code()} returns a single string.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
A Quarto executable cell parses to a \code{\link{pandoc_code_block}} whose engine
is carried as a braced class (\code{{r}}, \code{{python}}, \code{{ojs}}, ...) and whose
cell options live in leading \verb{#|} (or \verb{//|} / \verb{--|}) comment lines
inside \verb{@text}. None of that is surfaced by the raw node, so these
helpers read and edit it: they are the q2r analog of mq's \code{.code(lang)}
selector and of parsermd's \code{rmd_node_engine()} / \code{rmd_node_label()} /
\code{rmd_node_options()} family.

A plain fenced code block (\verb{```r}, class \code{r} with no braces) is \emph{not}
a cell: \code{is_code_cell()} is \code{FALSE}, \code{cell_engine()} is \code{NA}, and
\code{cell_options()} is empty.
}
\section{Helpers}{

\itemize{
\item \code{is_code_cell(x)} \code{TRUE} when \code{x} is an executable cell (a
\code{\link{pandoc_code_block}} with a braced engine class).
\item \code{cell_engine(x)} the engine name (\code{"r"}, \code{"python"}, ...) or \code{NA}.
\item \code{cell_label(x)} the \code{label} option, falling back to \verb{@attr@id}, or
\code{NA}.
\item \code{cell_code(x)} the cell body with the leading option lines stripped.
\item \code{cell_options(x)} the \verb{#|} options parsed from YAML into a named
list.
\item \code{set_cell_options(x, ...)} returns a new cell with options set from
named \code{key = value} pairs (a \code{NULL} value removes that option,
mirroring \code{\link[=set_attr]{set_attr()}}).
\item \code{set_cell_label(x, value)} convenience for \code{set_cell_options(x, label = value)}.
\item \code{collect_code(x, ...)} tangles the code of every cell under \code{x} into
one string.
}

Inside a \code{\link[=select_nodes]{select_nodes()}} / \code{\link[=map_nodes]{map_nodes()}} predicate the mask also
exposes \code{is_code_cell()} (zero-argument, tests the current node) and
\code{has_option(key)} / \code{has_option(key, value)}, so
\code{select_nodes(doc, is_code_cell() & has_option("eval", FALSE))} works.
}

\examples{
\dontrun{
doc = parse_qmd("```{r}\n#| label: fig-1\n#| echo: false\nplot(1)\n```\n")
cell = select_first(doc, is(pandoc_code_block))
cell_engine(cell)
cell_options(cell)
set_cell_options(cell, echo = TRUE, eval = NULL)
}

}
