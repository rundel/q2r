% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_qmd}
\alias{render_qmd}
\title{Render an AST to an output document with Quarto}
\usage{
render_qmd(x, output_file = NULL, output_format = NULL, ..., quiet = FALSE)
}
\arguments{
\item{x}{A \code{\link{pandoc}} or \code{\link{ts_tree}} AST.}

\item{output_file}{Destination path for the rendered output. Its
directory and base name are used; the extension follows from the
rendered format. If \code{NULL}, the output is written to the working
directory as \verb{document.<ext>}.}

\item{output_format}{Quarto output format (e.g. \code{"html"}, \code{"pdf"}),
passed to \code{\link[quarto:quarto_render]{quarto::quarto_render()}}. \code{NULL} uses the document /
Quarto default.}

\item{...}{Further arguments passed to \code{\link[quarto:quarto_render]{quarto::quarto_render()}}.}

\item{quiet}{Suppress Quarto's rendering output.}
}
\value{
The path to the rendered output file, invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Writes an in-memory AST back to QMD with \code{\link[=to_qmd]{to_qmd()}} and renders it
with \code{\link[quarto:quarto_render]{quarto::quarto_render()}}, the q2r analog of parsermd's
\code{render()}. The document is rendered inside a temporary directory and
the resulting artifacts (the rendered file plus any \verb{*_files/}
resource directory) are copied to the destination, so it never
clobbers your working directory mid-render.

Because rendering happens in a temporary directory, relative resource
references in the document (local images, includes, \verb{\{\{< include >\}\}}
shortcodes) will not resolve. This wrapper is intended for
self-contained documents.

Requires the \code{quarto} package (in \code{Suggests}) and a Quarto
installation.
}
\examples{
\dontrun{
doc = parse_qmd("# Title\n\nHello world.\n")
render_qmd(doc, "hello.html")
}

}
