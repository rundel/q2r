% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pampa.R
\name{pampa_parse}
\alias{pampa_parse}
\title{Parse QMD input with pampa}
\usage{
pampa_parse(input, ast = c("pd", "ts"), quiet = FALSE, prune_errors = TRUE)
}
\arguments{
\item{input}{A single string. Treated as a file path if it does not
contain newlines and \code{file.exists()} returns TRUE; otherwise
treated as raw text. To parse an R-held \code{\link{ts_tree}} as Pandoc,
render it first with \code{\link[=to_qmd]{to_qmd()}} and feed the result back in.}

\item{ast}{The AST to return: \code{"pd"} (the default) for the Pandoc AST
as a \code{\link{pandoc}} object, or \code{"ts"} for the tree-sitter AST as a
\code{\link{ts_tree}}.}

\item{quiet}{If \code{FALSE} (the default) any error-kind diagnostics are
raised as R errors (after attaching diagnostics to the result),
and warning-kind diagnostics are emitted as R warnings. If \code{TRUE}
no signal is raised; diagnostics are still attached to the
returned object's \verb{@diagnostics} slot.}

\item{prune_errors}{If \code{TRUE} (the default, matching the pampa CLI)
parser-error diagnostics are deduplicated by tree-sitter \code{ERROR}
node, keeping the earliest per node. Set to \code{FALSE} to see every
raw diagnostic pampa produces (useful for debugging the parser).}
}
\value{
For \code{ast = "pd"} a \code{\link{pandoc}} object, for \code{ast = "ts"} a
\code{\link{ts_tree}} object, with any parse diagnostics attached in the
\verb{@diagnostics} slot. If pampa fails to produce a Pandoc AST the
returned \code{pandoc} has an empty \verb{@blocks}; the diagnostics explain
why. Tree-sitter parsing itself never fails.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Parses QMD text or a file with pampa and returns the requested AST.
With \code{ast = "pd"} (the default) the Pandoc AST is returned as a
\code{\link{pandoc}} object; with \code{ast = "ts"} the tree-sitter concrete syntax
tree is returned as a \code{\link{ts_tree}}. Either way any parse diagnostics are
attached to the returned object's \verb{@diagnostics} slot.
}
