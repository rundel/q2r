% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast-filter.R
\name{ast_filter}
\alias{ast_filter}
\title{Apply a table of type-keyed handlers to a pandoc AST}
\usage{
ast_filter(x, ...)
}
\arguments{
\item{x}{A \code{\link{pandoc}} document or any \code{\link{pandoc_node}}.}

\item{...}{Named functions, where each name is an exported S7 class
(e.g. \code{pandoc_strong}, \code{pandoc_header}, \code{pandoc_block}).}
}
\value{
A rewritten value of the same outer shape as \code{x}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Borrows the Pandoc Lua filter idiom of dispatching on element type:
the user provides one named function per S7 class to rewrite, and
\code{ast_filter()} walks the AST once, invoking the matching handler at
each node. Handlers honour the same return-value contract as
\code{\link[=map_nodes]{map_nodes()}} (return a node to replace, a \code{list} of nodes to
splice, \code{NULL} to delete, or the input to no-op).

Dispatch is by S7 class with inheritance: a \verb{pandoc_block = \\(b) ...}
handler will run on every block subtype. When several handler names
match a node, the first listed in \code{...} wins, so put more specific
classes (e.g. \code{pandoc_header}) before more general ones
(\code{pandoc_block}).

Traversal is post-order: a parent's handler sees its children after
they have been rewritten. This matches Pandoc Lua filters' default.
}
\examples{
\dontrun{
doc = pampa_parse_pd("# Hello\n\n**bold** and *italic*\n")
doc |> ast_filter(
  pandoc_strong = \(el) pandoc_small_caps(el@content),
  pandoc_header = \(el) {
    if (el@level == 1L) {
      pandoc_header(level = 2L, content = el@content, attr = el@attr)
    } else el
  }
)
}

}
\seealso{
\code{\link[=map_nodes]{map_nodes()}} for predicate-based rewriting,
\code{\link[=ast_text]{ast_text()}} for stringifying a subtree.
}
