% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast-attr.R
\name{ast_attr}
\alias{ast_attr}
\alias{has_class}
\alias{add_class}
\alias{remove_class}
\alias{get_id}
\alias{set_id}
\alias{get_attr}
\alias{set_attr}
\title{Attribute manipulation helpers}
\usage{
has_class(x, cls)

add_class(x, cls)

remove_class(x, cls)

get_id(x)

set_id(x, id)

get_attr(x, key)

set_attr(x, ...)
}
\arguments{
\item{x}{A \code{\link{pandoc_node}} (typically one with an \verb{@attr} slot).}

\item{cls}{A character vector of class names.}

\item{id}{A single string.}

\item{key}{A single string naming an attribute (for \code{get_attr()}).}

\item{...}{For \code{set_attr()}, named \code{key = value} pairs; each \code{value}
is a single string, or \code{NULL} to remove that key. Quote names that
are not syntactic R identifiers (e.g. \code{"data-level" = "2"}), and
use rlang's \verb{!!!} / \verb{:=} to supply dynamic keys.}
}
\value{
Predicates return a logical scalar (or a string, for getters).
Setters return a node of the same class as \code{x}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Concise getters and setters for the \code{\link{pandoc_attr}} slot found on
many pandoc node types (\code{\link{pandoc_header}}, \code{\link{pandoc_div}},
\code{\link{pandoc_code}}, \code{\link{pandoc_link}}, \code{\link{pandoc_image}}, \code{\link{pandoc_span}},
etc.). Modelled on Pandoc Lua filters' direct field access
(\code{el.classes}, \code{el.identifier}, \code{el.attributes}) but using R's
immutable value semantics: every setter returns a new node, the
input is never modified.

Nodes without a standard \verb{@attr} slot are handled defensively: the
getters return \code{FALSE} / \code{""} / \code{NA} (including nodes such as ordered
and bullet lists, whose \verb{@attr} is a \code{pandoc_list_attributes} rather
than a \code{\link{pandoc_attr}}), while setters error with a clear message.
}
\section{Available helpers}{

\itemize{
\item \code{has_class(x, cls)} \code{TRUE} if any of \code{cls} is in \verb{@attr@classes}.
When used inside a \code{\link[=select_nodes]{select_nodes()}} / \code{\link[=map_nodes]{map_nodes()}} predicate,
the data-mask version is bound to the current node automatically;
the exported version here takes the node as its first argument.
\item \code{add_class(x, cls)} add one or more classes (idempotent: classes
already present are not duplicated).
\item \code{remove_class(x, cls)} remove one or more classes; no-op on
classes that are not present.
\item \code{get_id(x)} / \code{set_id(x, id)} read or replace \verb{@attr@id}.
\item \code{get_attr(x, key)} reads a single attribute, returning
\code{NA_character_} for missing keys.
\item \code{set_attr(x, key = value, ...)} sets one or more attributes from
named \code{key = value} pairs. A \code{value} of \code{NULL} removes that key
(the named map's idiomatic R removal, mirroring Lua's
\code{el.attributes[key] = nil}), so a single call can both set and
drop: \code{set_attr(x, target = "_blank", lang = NULL)}.
}
}

\examples{
\dontrun{
doc = parse_qmd("# title {.unnumbered}\n")
doc |> map_nodes(is(pandoc_header), .f = function(h) {
  h |> add_class("highlight") |> set_id("intro")
})
}

}
