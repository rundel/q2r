% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toc.R
\name{ast_toc}
\alias{ast_toc}
\title{Build a table of contents from a document's headings}
\usage{
ast_toc(x, max_level = 3L, ...)
}
\arguments{
\item{x}{A \code{\link{pandoc}} document, a \code{\link{pandoc_blocks}} wrapper, or a list
of blocks.}

\item{max_level}{Deepest heading level to include (default \code{3}).}

\item{...}{Unused; for future extension.}
}
\value{
A \code{\link{pandoc_bullet_list}} block (empty when there are no
qualifying headings).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Walks the headings of a \code{\link{pandoc}} document in order and returns a
nested \code{\link{pandoc_bullet_list}} linking to each one, the q2r analog of
mq's \code{section::toc()}. The result is an ordinary block, so it can be
spliced into the document with \code{\link[=insert_after]{insert_after()}} or wrapped in a
\code{\link{pandoc}} and rendered with \code{\link[=to_qmd]{to_qmd()}}.

Each entry links to the heading's explicit identifier (\verb{@attr@id}) when
it has one; otherwise an approximate Pandoc-style slug of the heading
text is used. Headings deeper than \code{max_level} are omitted.
}
\examples{
\dontrun{
doc = parse_qmd("# A\n\n## B\n\ntext\n\n# C\n")
toc = ast_toc(doc)
doc@blocks@content = c(list(toc), doc@blocks@content)
}

}
