% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{node_predicates}
\alias{node_predicates}
\alias{has_id}
\alias{has_attr}
\alias{has_text}
\alias{has_label}
\title{Node-level predicate helpers}
\usage{
has_id(x, id)

has_attr(x, key, value)

has_text(x, pattern, fixed = FALSE)

has_label(x, pattern)
}
\arguments{
\item{x}{A pandoc AST node.}

\item{id}{The expected \verb{@attr@id} string.}

\item{key}{The attribute key to look up.}

\item{value}{The expected attribute value; when missing, tests for
presence of the key only.}

\item{pattern}{For \code{has_text()}, regular expression(s) matched against
the node's \code{\link[=ast_text]{ast_text()}}; for \code{has_label()}, glob pattern(s) matched
against the node's label (\verb{@attr@id}, or a code cell's \code{label}
option).}

\item{fixed}{Treat \code{pattern} as a literal string.}
}
\value{
A single logical.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Node-first versions of the predicate-mask helpers from
\code{\link[=select_nodes]{select_nodes()}}, usable as ordinary functions on a node you already
hold (the zero-node forms of the same names remain available inside
predicates). All return a single \code{TRUE}/\code{FALSE}, and are silently
\code{FALSE} on nodes without the relevant slot (including the whole
tree-sitter AST). \code{has_option()} / \code{has_engine()} are documented with
the other cell helpers in \code{\link[=is_code_cell]{is_code_cell()}}.
}
