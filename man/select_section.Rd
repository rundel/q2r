% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select-section.R
\name{select_section}
\alias{select_section}
\title{Select the blocks belonging to a heading section}
\usage{
select_section(x, path, levels = 1:6, include_heading = TRUE)
}
\arguments{
\item{x}{A \code{\link{pandoc}} document, a \code{\link{pandoc_blocks}} wrapper, or a
plain \code{list} of top-level blocks.}

\item{path}{Character vector of glob patterns naming the heading
chain to match, outermost first.}

\item{levels}{Integer vector of heading levels (1-6) that count as
section boundaries. Defaults to all levels.}

\item{include_heading}{Whether to include the matched heading block
itself in the result (default \code{TRUE}).}
}
\value{
A \code{list} of blocks (consistent with \code{\link[=select_nodes]{select_nodes()}}).
Wrap it in \code{pandoc(blocks = pandoc_blocks(result))} to render it
with \code{\link[=to_qmd]{to_qmd()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Slices the contiguous run of top-level blocks introduced by a
heading whose title (and ancestor titles) match \code{path}. This is the
Pandoc analog of parsermd's \code{by_section()}: where \code{select_nodes()}
can find the \code{\link{pandoc_header}} node, \code{select_section()} returns the
header together with everything beneath it, up to the next heading of
equal or higher level.

\code{path} is a character vector of glob patterns, outermost heading
first, matched against the enclosing-heading chain from
\code{\link[=ast_sections]{ast_sections()}} (collapsed to the non-\code{NA} levels selected by
\code{levels}). A section anchor matches when its collapsed heading chain
has the same length as \code{path} and each element glob-matches, so
\code{path = c("Results", "Model *")} selects each \verb{Model *} subsection
nested directly under a \code{Results} heading. Headings whose level is
not in \code{levels} are treated as ordinary content.

Section membership depends on document order, which a per-node
predicate (evaluated on one node in isolation) cannot see; that is
why this is a dedicated verb rather than a \verb{has_*()} mask helper.
}
\examples{
\dontrun{
doc = parse_qmd("# Intro\n\na\n\n## Setup\n\nb\n\n# Other\n\nc\n")
select_section(doc, "Intro")
select_section(doc, c("Intro", "Setup"), include_heading = FALSE)
}

}
