% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ast-sections.R
\name{ast_sections}
\alias{ast_sections}
\title{Heading-section path for each top-level block}
\usage{
ast_sections(x, ...)
}
\arguments{
\item{x}{A \code{\link{pandoc}} document, a \code{\link{pandoc_blocks}} wrapper, or a
plain \code{list} of top-level blocks.}
}
\value{
A \code{list} with one element per top-level block, each a named
character vector \code{c(h1, h2, h3, h4, h5, h6)} with \code{NA} where no
heading at that level is in scope.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Walks the top-level block stream of a \code{\link{pandoc}} document and, for
each block, reports the chain of enclosing \code{\link{pandoc_header}} titles.
Because the Pandoc block list is flat (headings are siblings of the
content they introduce, not parents of it), this is the piece of
structure that q2r does not otherwise expose. It is the analog of
parsermd's \code{rmd_node_sections()} and the basis for
\code{\link[=select_section]{select_section()}}.

Each block is assigned a length-6 named character vector
(\code{h1}..\code{h6}). A header is considered part of the section it opens, so
a level-2 header \verb{## Methods} carries \code{h2 = "Methods"} itself and
resets any deeper (\code{h3}..\code{h6}) entries. Heading titles are the
flattened text of the header (\code{\link[=ast_text]{ast_text()}}).
}
\examples{
\dontrun{
doc = parse_qmd("# A\n\nintro\n\n## B\n\nbody\n")
ast_sections(doc)
}

}
